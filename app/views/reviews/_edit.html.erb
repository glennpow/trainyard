<%= f.hidden_field(:user_id) %>
<%= render_field t(:author, :scope => [ :content ]), review.user.name %>

<%= f.rating_select :rating %>

<%= f.text_area :body, :preview => true, :hint => link_to_text_style_hint %>
