<% title t(:regarding_topic, :scope => [ :content ], :topic => h(@topic.name)) %>

<% form_for :post, :url => objects_path do |f| %>
  <%= render :partial => 'edit', :locals => { :f => f, :post => @post } %>

  <%= f.submit_with_cancel(:submit => t(:create)) %>
<% end %>

