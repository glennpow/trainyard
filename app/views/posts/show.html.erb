<% title h(@post.topic_name) %>

<%= render_field(t(:author, :scope => [ :content ]), link_to_user(@post.user)) %>

<% if @post.guru_points > 0 %>
  <%= render_field(tp(:guru_point, :scope => [ :content ]), @post.guru_points) %>
<% end %>

<%= render_text_area(@post.body) %>
