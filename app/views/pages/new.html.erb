<% title t(:create_object, :object => t(:page, :scope => [ :content ])) %>

<% form_for :page, :url => objects_path do |f| %>
  <%= render :partial => 'pages/edit', :locals => { :f => f, :page => @page } %>

  <%= f.submit_with_cancel(:submit => t(:create)) %>
<% end %>
