<% title t(:edit_object, :object => t(:forum, :scope => [ :forums ])) %>

<%= render_heading(t(:edit_object, :object => t(:forum, :scope => [ :forums ]))) %>

<% form_for :forum, :url => object_path, :html => { :method => :put } do |f| %>
  <%= render :partial => 'edit', :locals => { :f => f, :forum => @forum } %>

  <%= f.submit t(:save) %>
<% end %>

<%= render_page_actions([ { :allow => link_to_back } ]) %>
