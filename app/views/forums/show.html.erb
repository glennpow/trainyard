<% title "#{t(:forum, :scope => [ :content ])}: #{h(@forum.name)}" %>

<%= render :partial => 'show', :locals => { :forum => @forum } %>

<%= render_indexer(@topics_indexer) %>

<% page_action icon_link_to(:add_large, t(:new_object, :object => t(:topic, :scope => [ :content ])), new_forum_topic_path(@forum)) %>
