<% title tp(:forum, :scope => [ :content ]) %>

<%= render_heading(tp(:forum, :scope => [ :content ])) %>

<% if has_administrator_role? %>
  <% @indexer.footers = link_to(t(:create_object, :object => t(:forum, :scope => [ :content ])), new_forum_path) %>
<% end %>

<%= render_indexer(@indexer) %>
