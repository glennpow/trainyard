<%= render_row(
  indexer,
  article_revision,
  [
    link_to_resource(article_revision),
    link_to_resource(article_revision.user),
    article_revision.revision,
    h(article_revision.created_at.to_s(:short)),
  ]) %>
