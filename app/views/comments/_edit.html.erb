<% if comment.user %>
  <%= f.hidden_field(:user_id) %>
  <%= show_field t(:author, :scope => [ :content ]), comment.user.name %>
<% else %>
  <%= f.text_field :user_name, :label => { :name => t(:author, :scope => [ :content ]) } %>
<% end %>

<%= f.text_area :body %>
