<%= render_heading(t(:comments_for_object, :scope => [ :content ], :count => 1, :object => link_to_resource(comment.resource))) %>

<%= render_field(t(:author, :scope => [ :content ]), comment_author(comment)) %>

<%= render_text_area(comment.body) %>