<% if @forum %>
  <% title h(@forum.name) %>
  <%= render :partial => 'forums/show', :locals => { :forum => @forum } %>
<% else %>
  <% title tp(:topic, :scope => [ :content ]) %>
<% end %>

<%= render_indexer(@indexer) %>
