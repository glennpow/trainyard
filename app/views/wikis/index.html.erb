<% if @group %>
  <% title t(:object_for_parent, :object => tp(:wiki, :scope => [ :content ]), :count => @indexer.collection.total_entries, :parent => @group.name) %>
<% else %>
  <% title tp(:wiki, :scope => [ :content ]) %>
<% end %>

<%= render_indexer(@indexer) %>

<% if is_editor_of?(@group) %>
  <% page_action icon_link_to(:add_large, t(:add_object, :object => t(:wiki, :scope => [ :content ])), new_object_path) %>
<% end %>
