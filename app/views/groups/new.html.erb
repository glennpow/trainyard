<% title t(:create_object, :object => t(:group, :scope => [ :authentication ])) %>

<% form_for :group, :url => objects_path do |f| %>
  <%= render :partial => 'edit', :locals => { :f => f, :group => @group, :parent_groups => @parent_groups } %>

  <%= f.submit_with_cancel(:submit => t(:create)) %>
<% end %>
