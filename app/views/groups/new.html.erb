<% title t(:create_object, :object => t(:group)) %>

<% form_for :group, :url => objects_path do |f| %>
  <%= render :partial => 'edit', :locals => { :f => f, :group => @group, :parents => @parents } %>

  <%= f.submit_with_cancel(:submit => t(:create)) %>
<% end %>
