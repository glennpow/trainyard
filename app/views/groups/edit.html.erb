<% title t(:edit_object, :object => t(:group)) %>

<% form_for :group, :url => object_path, :html => { :method => :put } do |f| %>
  <%= render :partial => 'edit', :locals => { :f => f, :group => @group, :parent_groups => @parent_groups } %>

  <%= f.submit_with_cancel(:submit => t(:save)) %>
<% end %>
