<% title tp(:group, :scope => [ :authentication ]) %>

<%= render_heading(tp(:group, :scope => [ :authentication ])) %>

<% if has_administrator_role? %>
  <% @indexer.footers = link_to(t(:create_object, :object => t(:group, :scope => [ :authentication ])), new_group_path) %>
<% end %>

<%= render_indexer(@indexer) %>
