<% title h(@media.name) %>

<%= render_field(t(:group, :scope => [ :authentication ]), link_to_resource(@media.resource)) %>

<%= render_field(t(:date, :scope => [ :datetimes ]), h(@media.created_at)) %>

<%= render :partial => 'show', :locals => { :media => @media } %>

<% page_action icon_link_to(:edit, t(:edit), edit_object_path) if is_editor_of?(@media) %>
<% page_action link_to_resource(@media.resource) %>
