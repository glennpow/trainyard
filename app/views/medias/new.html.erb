<% title t(:create_object, :object => t(:media, :scope => [ :content ])) %>

<% form_for(:media, :url => objects_path) do |f| %>
  <%= render :partial => 'edit', :locals => { :f => f, :media => @media } %>
  
  <%= f.submit_with_cancel(:submit => t(:create)) %>
<% end %>
