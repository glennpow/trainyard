<% title t(:object_for_parent, :object => t(:media, :scope => [ :content ]), :parent => @resource.name) %>

<%= render_indexer(@indexer) %>

<% if is_editor_of?(@resource) %>
  <% page_action icon_link_to(:add_large, t(:add_object, :object => t(:media, :scope => [ :content ])), new_object_path) %>
<% end %>
