<% title t(:edit_object, :object => t(:media, :scope => [ :content ])) %>

<% form_for :media, :url => object_path, :html => { :method => :put } do |f| %>
  <%= render :partial => 'edit', :locals => { :f => f, :media => @media } %>

  <%= f.submit_with_cancel(:submit => t(:save)) %>
<% end %>
