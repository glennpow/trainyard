<% title t(:edit_object, :object => t(:blog, :scope => [ :content ])) %>

<% form_for :blog, :url => object_path, :html => { :method => :put } do |f| %>
  <%= render :partial => 'edit', :locals => { :f => f, :blog => @blog } %>

  <%= f.submit_with_cancel(:submit => t(:save)) %>
<% end %>
