<% title h(@blog.name) %>

<%= render_heading(h(@blog.name)) %>

<%= render_indexer(@articles_indexer) %>

<% page_actions << icon_link_to(:edit, t(:edit), edit_object_path) if is_editor_of?(@blog) %>
