<% title t(:create_object, :object => t(:blog, :scope => [ :content ])) %>

<% form_for(:blog, :url => objects_path) do |f| %>
  <%= render :partial => 'edit', :locals => { :f => f, :blog => @blog } %>
  
  <%= f.submit_with_cancel(:submit => t(:create)) %>
<% end %>
