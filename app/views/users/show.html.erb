<% title t(:user, :scope => [ :authentication ]) %>

<%= render :partial => 'show', :locals => { :user => @user } %>

<% page_actions << link_to(t(:send_message, :scope => [ :content ]), new_message_to_user_path(@user)) if @user != current_user %>
