<% title @message.subject %>

<%= render :partial => 'show', :locals => { :message => @message } %>

<% page_actions << link_to(t(:reply), new_message_to_user_path(@message.from_user)) if @message.is_to?(current_user) %>
